{
  "name": "arka-client",
  "version": "0.1.0",
  "description": "Reference static web client for arkA protocol",
  "scripts": {
    "lint:schema": "ajv validate -s schema/video.json -d examples/*.json",
    "build": "rollup -c",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "ajv": "^8.0.0",
    "rollup": "^3.0.0",
    "gh-pages": "^5.0.0"
  }
}