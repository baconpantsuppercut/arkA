{
  "name": "arka",
  "version": "0.1.0",
  "private": true,
  "description": "arkA – an open video protocol (metadata, indexes, and reference clients).",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}