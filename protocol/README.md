# Arka Protocol

This folder will contain the protocol specification for Arka:
- Core data models (Video, Creator, Playlist)
- How storage URIs are referenced
- How clients discover and fetch content

MVP goal: define a simple JSON schema for the Video object and a static index format.